{
  "id": "WP-008",
  "title": "Find the authorization assertions that cannot fail",
  "objective": "Mutation-test every policy the seven pgTAP suites claim to guard, and find the assertions that pass because a sibling policy hides the row rather than because the policy under test refuses. One was found by accident; nobody knows how many others there are.",
  "tasks": [
    "T099"
  ],
  "acceptance_criteria": [
    "every RLS policy in supabase/migrations/ is mutated one at a time — weakened to a permissive predicate, never deleted — and the suites run against each mutation",
    "a table in docs/ops/ lists each policy against the assertions that go red when it is weakened, and names every policy for which NO assertion goes red",
    "each masked assertion is either moved to a fixture where the policy under test is the only guard, or renamed and commented to say what it actually covers and which assertion covers the rest",
    "no policy is weakened in a committed file — every mutation is reverted, and the final ./scripts/verify.sh runs against the unmutated tree",
    "the report states how many assertions were checked and how many mutations were run, so a later reader can tell coverage from a spot check",
    "DONE 2026-08-06, ahead of the rest of this package, on founder instruction: TRUNCATE, REFERENCES, TRIGGER and MAINTAIN are revoked from anon, authenticated AND service_role on every table in public, the default privilege that produced them is revoked so the next table does not inherit them, and data_api_grants_test.sql gained four named assertions plus a dynamic guard that names any table/role/privilege still holding one. Seen to fail first: the local harness was quieter than production, so the negative assertion could not fail until local-db-bootstrap.sql was made to reproduce production's default privilege."
  ],
  "dependencies": [],
  "scope": {
    "files": [
      "docs/ops/",
      "supabase/tests/"
    ],
    "shared_files": [
      "supabase/tests/",
      "supabase/migrations/",
      "docs/ops/",
      "specs/003-meal-publishing/tasks.md"
    ]
  },
  "execution_mode": "PARALLEL",
  "status": "IN_PROGRESS",
  "owner": "coordinator",
  "priority": 3,
  "spend_envelope_usd": 3.0,
  "suggested_model": "opencode-go/grok-4.5",
  "branch": "claude/caveman-full-7k5toq",
  "pr": null,
  "blocked_reason": null,
  "notes": "Found during WP-006. The assertion 'non-owner cannot write another Cook's address form' passed with the UPDATE policy weakened to USING (true), because that fixture's kitchen has no Meal on offer and the SELECT policy refuses the statement before the UPDATE policy is consulted. kitchen_profiles_rls_test.sql had PREDICTED this at case 3, in a comment written while kitchens were still private: the masking would begin the day open kitchens became publicly readable. E2 shipped that change and nothing re-read the warning. So this is not a hunt for a hypothetical — it is one confirmed instance plus a documented mechanism for producing more, and 76 assertions nobody has mutation-tested. EXCLUSIVE because it edits supabase/tests/ across every suite and touches migrations transiently; two workers mutating policies against the same database would read each other's mutations as their own results. THE WHOLE PACKAGE IS A READ OF EXISTING GUARANTEES, NOT A WIDENING: if a masked assertion is found, the fix is to move the test, never to change the policy. A policy change coming out of this package is a stop-and-report to the founder.\n\nUPDATE 2026-08-06: the TRUNCATE criterion was landed on its own, because it was the one item the founder had held back for approval and he gave it. The rest of this package — mutation-testing 76 assertions across seven suites — is untouched and is still what the package is for. Nothing about the revoke changes that work: it was a grant-layer narrowing, not a policy change, and the masked-assertion problem lives entirely in the policy layer.\n\nEXCLUSIVE DROPPED 2026-08-06, coordinator's decision, with the reasoning because the flag was set on a premise that does not hold. It read 'two workers mutating policies against the same database would read each other's mutations as their own results'. Sessions run in separate containers and scripts/local-db.sh builds a Postgres inside each one, so there is no shared database to race on; this package never touches production or a preview branch. The file reason — editing supabase/tests/ across every suite — is real, and no remaining package touches supabase/tests/ at all. What genuinely collides is docs/ops/ and specs/*/tasks.md, where every package adds its own file or its own block; those are declared in shared_files instead of shutting the whole repository down for one package.",
  "created_at": "2026-08-05T18:40:00Z",
  "updated_at": "2026-08-06T07:10:00Z"
}
