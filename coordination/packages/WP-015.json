{
  "id": "WP-015",
  "title": "Let a Customer ask for food in their own words",
  "objective": "The read path: one sentence in, ranked Meals out, rendered before anything is judged. Plus area narrowing and the disclosure that a Customer's words leave Kafoo.",
  "tasks": [
    "T141",
    "T142",
    "T143",
    "T144",
    "T145",
    "T146",
    "T147",
    "T148",
    "T149",
    "T150",
    "T151",
    "T152",
    "T153",
    "T154",
    "T155",
    "T207",
    "T208",
    "T209",
    "T210",
    "T212",
    "T213",
    "T215",
    "T216",
    "T221",
    "T222",
    "T223",
    "T224",
    "T225"
  ],
  "acceptance_criteria": [
    "discover holds NO service-role key and passes the caller's credentials through to search_meals",
    "phrase parsing is deterministic with no model call — a generative call here would put the model on the critical path FR-011 keeps it off",
    "results render with no await on any judgement (FR-011), proven by a test that stubs the judgement to hang",
    "burger typed in Latin script returns the Arabic Meal at rank 1; SC-002 and SC-003 verified by name",
    "an empty named area says so AND names the areas that do have food, and the Customer must choose (FR-024, FR-024a). No distance is stated and no deliverability implied (FR-024b, FR-024c)",
    "SearchPerformed carries result_count and NO phrase reaches the event, a log line, or an error carrying the request",
    "./scripts/verify.sh passes",
    "a Customer is asked ONCE, at the first attempt to search and never on arrival, and is never asked again either way (SC-015)",
    "the answer is stored on the DEVICE, not by Kafoo — Kafoo holds no record of a Customer and discovery works signed-out (FR-029d)",
    "with the switch off, ZERO words leave the device, verified by watching what leaves rather than by reading the code that decides (SC-014)",
    "with the switch off, search is unavailable rather than degraded, and there is no reduced search matching on spelling (FR-029e)"
  ],
  "dependencies": [
    "WP-013",
    "WP-014"
  ],
  "scope": {
    "files": [
      "supabase/functions/discover/",
      "apps/mobile/lib/features/discovery/",
      "apps/mobile/lib/features/settings/"
    ],
    "shared_files": [
      "apps/mobile/lib/l10n/app_ar.arb",
      "apps/mobile/lib/l10n/app_en.arb",
      "apps/mobile/lib/features/discovery/data/discovery_repository.dart"
    ]
  },
  "execution_mode": "PARALLEL",
  "status": "READY_FOR_REVIEW",
  "owner": "session_01SftX86m6ninrQ4Y9p4rTzM",
  "priority": 3,
  "spend_envelope_usd": 3.0,
  "suggested_model": "opencode-go/qwen3.6-plus",
  "branch": "claude/e3-wp015-customer-search-ce8oif",
  "pr": null,
  "blocked_reason": null,
  "notes": "The largest package in E3. T191 is a founder decision that blocks T215 — whether a Customer may refuse the disclosure and fall back to browsing — and it must be asked at the START of this package, not discovered at the end of it. The disclosure moved here from Polish after analyze found search would otherwise reach Customers a whole phase before they were told their words leave Kafoo.\n\nT191 is answered, founder 2026-08-06: a Customer may ALWAYS refuse. Asked once at the first search, remembered on the device, changeable from a Settings switch, and refusing falls back to browsing. T223 adds Kafoo's FIRST Settings screen and a settings toggle — two stop-and-ask triggers, both asked for explicitly by the founder. Keep it to the one switch rather than letting it become a home for future toggles.\n\nThe non-obvious part, and the thing a worker will get wrong: OFF means search is UNAVAILABLE, not 'search without AI'. A phrase leaves Kafoo to become searchable at all — that is the embedding, not the judgement — so there is no halfway state. And no ILIKE fallback: the rules forbid it by name and a worse search is not a kindness.\n\nDELEGATION SUSPENDED 2026-08-06 by the founder: the OpenCode weekly limit is reached. Do this package's work directly and do NOT dispatch to opencode-delegate or claude-delegate until the founder says otherwise. suggested_model and spend_envelope_usd are kept as the plan for when it resumes; they are not permission to spend today.\n\nSTARTED 2026-08-07 on branch claude/e3-wp015-customer-search-ce8oif, restarted from main (60c1268). Written directly — delegation is still suspended.\n\nDELIVERED so far, gate green on each commit:\n- The area is read out of the ONE sentence in `discover`, never normalised there (normalise_area stays in SQL), cut at a negation marker, and an explicit `area` in the request overrides it so FR-024a's 'the Customer chooses' is reachable.\n- `detail: String(error)` removed from discover's 503. A provider rejecting a request answers with a body that quotes the request back, and translateFailure puts that body into the message — so the phrase could ride out inside an error. Seen to fail before the fix.\n- The search screen and the consent gate, built as one thing. The gate is in SearchController.search — the only door to the network — rather than in the screen, because a screen check is one branch among several and the next entry point walks past it.\n- Kafoo's first Settings screen, one switch, reversible both ways.\n- Voice where the device supports it, typing where it does not, and the microphone passes the same gate as typing.\n- The backfill script, with an interrupted run leaving Meals harder to find rather than lost.\n- T216: a Meal that went off offer between being ranked and opened says so, on top of the Meal rather than instead of it.\n- WP-017's last criterion: searchFilteredOn states what was removed and where the information came from, and never that a Meal is safe.\n\nTHE PROVIDERSCOPE THE APP DID NOT HAVE. Browse became the signed-out home in E3 as a ConsumerWidget and runApp had no ProviderScope, so the app threw on its first frame for anyone without an account. Nothing caught it: app_test skips KafooApp because _AuthGate needs a live Supabase, and every widget test builds its own scope. A screen tested in isolation is not a screen that has been run.\n\nNOT DONE, AND NEEDING SOMETHING THIS SESSION CANNOT SUPPLY:\n- T153 (promote the retrieval spike to a regression, gate or nightly) is a FOUNDER DECISION: it calls a vendor and costs money, which verify.sh has never done.\n- T154 (measure search latency against production) and T155 (verify SC-002 and SC-003 against the corpus) need the deployed function and real model calls. The paid tier is still off.\n\n\nREVIEWED 2026-08-07 by trust-reviewer and localization-reviewer, both of which RAN the code. Eight defects, all fixed, all with a test seen to fail first:\n- `chooseArea` reached the network WITHOUT passing the consent gate. The gate was on `search`, an entrance, and FR-024a's area choice is a second entrance. The old SC-014 test passed vacuously because chooseArea with no earlier search returns before touching anything. The gate is now on the funnel.\n- The empty-area state said the marketplace was empty whenever browse was still loading or had failed. The shipped fake fails browse and search together, which is why nobody saw it.\n- The consent question came back after Settings answered it, with a live 'No' button on top of a granted answer — down the exact route the copy under the question tells a Customer to take.\n- CRITICAL, from localization-reviewer: six Customer-facing sentences rendered in MaterialApp's ERROR STYLE — 48-point monospace under a yellow double underline, 966 pixels tall on a 360dp screen at ORDINARY text scale. `DefaultTextStyle.of(context)` inside a State resolves above the Scaffold. Twenty-five widget tests passed over it: find.text does not look at style, and nothing in this repository set a text scale or a phone-sized viewport. Four tests now do.\n- Agreeing was a FilledButton and refusing an OutlinedButton while three places asserted equal prominence.\n- `notUnderstood` carried the whole tail of the Customer's sentence across the network and into both 503 bodies, for an interface that only read whether it was set. A boolean now.\n- Copy: pronoun disagreement in the allergy sentence for seven of twelve exclusions; 'shellfish' rendered as a word that includes fish; the consent reassurance scoped ambiguously to the outside service; MSA subjunctive; two calques.\n- `searchLabel` was spelled identically for a woman and a man and PRONOUNCED nafsak/nafsik — and a TextField label is read aloud. ADR-0010's 'spelled identically' test was made for reading.\n\nWP-017's LAST CRITERION IS NOW MET — the interface states what it filtered on and never that a Meal is safe. WP-017 belongs to another session, so its status is not changed here.\n\nTHREE THINGS NEEDING THE FOUNDER, none blocking a merge:\n1. VOICE SENDS AUDIO OFF THE DEVICE. `speech_to_text` defaults to `onDevice: false`, so a Customer's recording goes to the handset's speech service — a third party that is neither Kafoo nor the embedding provider. The consent copy now says so. Setting `onDevice: true` would keep it local and would make the microphone silently useless on handsets with no on-device ar-EG, which is most of them. It also affects the Cook's conversation, which has shipped this way since E1.\n2. `searchLabel` is now 'أكل إيه النهاردة؟', which addresses nobody and is colder than the 'نفسك في إيه؟' it replaces. Worth hearing both read aloud.\n3. The AI Assistant has two Arabic names in the ARB file — five existing strings say المساعد الذكي. The new strings were changed to match rather than introduce a second. مساعد كفو is arguably better register; switching is a seven-string sweep.\n",
  "created_at": "2026-08-06T18:00:00Z",
  "updated_at": "2026-08-07T12:30:00Z"
}
