name: kafoo_ai
description: Provider abstraction, prompts, and conversation engine. The only package that may import a vendor SDK.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.6.0

dependencies:
  kafoo_domain:
    path: ../domain

dev_dependencies:
  test: ^1.25.0
