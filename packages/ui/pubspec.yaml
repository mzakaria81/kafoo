name: kafoo_ui
description: Shared widgets and the Kafoo design system.
version: 0.1.0
publish_to: 'none'

environment:
  flutter: '>=3.27.0'
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
