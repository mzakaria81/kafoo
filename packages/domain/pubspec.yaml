name: kafoo_domain
description: Kafoo entities and pure business logic. No Flutter, no Supabase.
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.6.0

dev_dependencies:
  test: ^1.25.0
