# Measuring E2 performance and per-Meal model cost

Generated by `scripts/measure-e2-performance.ts` on 2026-08-05.

- **Model tier**: fast, resolved by the registry (the fast tier as the registry resolved it)
- **Analysis runs**: 12
- **Publish runs**: 12
- **Cost runs**: 8 fixtures + 1 image call
- **Request spacing**: 4500 ms (free tier allows 15 requests per minute)

## Where these numbers were taken, and what is still missing

**Measured against the pull request's Supabase preview branch, not production.** The live project
grants no application role any DML privilege — every call returns `42501 permission denied` before
Row Level Security is reached — so no Cook can create a Meal there.
`supabase/migrations/20260805180727_grant_data_api_privileges.sql` fixes that, and the preview
branch is a real Supabase with that migration already applied: real Auth, real PostgREST, real Edge
Functions. Same stack, different and colder project.

**Confirm → on-offer is measured. Description-finished → first estimate is not**, and the reason is
specific rather than general: all twelve calls returned `HTTP 502 provider_misconfigured`. Supabase
does not copy a parent project's secrets into a preview branch, so `analyze-meal` has no model
credential there and fails at provider resolution, before it ever calls a model. Adding that secret
was refused by a permission boundary in the session that ran this, and rightly — it is a credential
move.

### What the missing number probably looks like

Arithmetic, not a measurement. Each part below was measured; they were never measured together in
one span, and a sum of three medians is not a median of the sum:

| Part | Median | Runs |
|---|---|---|
| Persisting the description | 220 ms | 12 |
| The Edge Function before the model call (auth, ownership check) — what the failed calls timed | 740 ms | 12 |
| One model call | 1397 ms | 8 |
| **Composed** | **≈2.4 s** | — |

**Against a 2-second budget, that misses.** Two reasons to hold off acting on it. The model call was
timed from this container straight to the provider, while in production the Edge Function makes that
call from Supabase's own network — a different path that could go either way. And a preview branch
is cold in a way a used production project is not.

**What closes it:** put a model-provider credential on a preview branch, or land the grant migration
and run this against production. Then re-run this script; it will replace this section with a
measurement.

## How this was measured

**Description-finished → first estimate:** Two round trips, both timed. The app persists the
description and only starts the analysis once that write succeeds, so the Cook waits for both:
the PostgREST PATCH that stores the description, then the HTTPS POST to the `analyze-meal`
Edge Function. The clock starts before the PATCH and stops after the analysis body is
read to completion. This includes the full network round trip, the Edge Function
cold start (if any), the model call, and Row Level Security on the Meal ownership
check. It does NOT include the Flutter widget rebuild after the reply arrives, nor
the time the Cook spends reading the estimate.

**Confirm → on-offer:** The timer starts immediately before the PostgREST PATCH that
sets the Meal status to `published` and stops after the response body is read to
completion. This includes the full network round trip and Row Level Security. It does
NOT include the Flutter widget rebuild or any database trigger work that fires after
the response is sent.

**Model cost:** The provider is called directly (not through the Edge Function) with
the same request shape the Edge Function uses. A wrapped `fetch` captures the raw
provider response to read token usage, which the adapter interface does not expose.

**Excluded from all measurements:**
- Flutter widget rebuild time after each reply arrives
- Network latency from an Egyptian mobile network (runs execute from a cloud container)
- The image call measures token cost of dimensions only — the image is a flat gray
  colour, not food, because Kafoo's rules forbid synthetic food imagery.

## Description-finished → first estimate

**NOT MEASURED in this run.** 12 run(s) were attempted and none returned
an analysis, so there is no number here — not a fast one and not a slow one. A call that
fails is not a call that is quick, and averaging failures in would make this figure improve
the more broken the feature was.

How they failed:

- 12 run(s): HTTP 502 — provider_misconfigured

The persist half of the span still completed on every run, so it is reported below on its
own. It is one half of a number, and it is labelled as one half.

- **Persisting the description**: median 220.0 ms across 12 runs (172–383 ms)
- **The failed call itself**: median 740.0 ms across 12 runs (498–2492 ms). This
  is not the analysis time. It is how long the Edge Function took to reach the point where
  it gave up, which for these failures is everything before the model call.

| Run | Fixture | Persist (ms) | Failed call (ms) | HTTP | Error |
|---|---|---|---|---|---|
| 1 | prompt injection inside macarona bechamel | 370 | 502 | 502 | provider_misconfigured |
| 2 | burger with transliterated English | 365 | 867 | 502 | provider_misconfigured |
| 3 | panee with mixed Latin and Arabic script | 380 | 498 | 502 | provider_misconfigured |
| 4 | koshary, plainly described | 178 | 2492 | 502 | provider_misconfigured |
| 5 | stuffed vegetables mahshi | 199 | 1140 | 502 | provider_misconfigured |
| 6 | molokhia with chicken and rice | 227 | 862 | 502 | provider_misconfigured |
| 7 | om ali dessert | 180 | 504 | 502 | provider_misconfigured |
| 8 | prompt injection inside macarona bechamel | 172 | 723 | 502 | provider_misconfigured |
| 9 | burger with transliterated English | 383 | 681 | 502 | provider_misconfigured |
| 10 | panee with mixed Latin and Arabic script | 182 | 842 | 502 | provider_misconfigured |
| 11 | koshary, plainly described | 213 | 730 | 502 | provider_misconfigured |
| 12 | stuffed vegetables mahshi | 337 | 750 | 502 | provider_misconfigured |

## Confirm → on-offer

- **Runs**: 12
- **Minimum**: 179 ms
- **Median**: 189.5 ms
- **Maximum**: 420 ms
- **Mean**: 224.9 ms
- **Budget**: 3000 ms
- **Verdict**: **PASS** — median 189.5 ms is within the 3000 ms budget across 12 runs.

| Run | Meal id (prefix) | Elapsed (ms) | HTTP |
|---|---|---|---|
| 1 | ccd99b1c… | 189 | 200 |
| 2 | ff3cf3e5… | 179 | 200 |
| 3 | 040cb038… | 204 | 200 |
| 4 | 3e225491… | 179 | 200 |
| 5 | 6dac1d4a… | 420 | 200 |
| 6 | ff903ef2… | 217 | 200 |
| 7 | 0fdbe5a5… | 184 | 200 |
| 8 | 1922804e… | 190 | 200 |
| 9 | 740447d5… | 179 | 200 |
| 10 | 79a3ba41… | 192 | 200 |
| 11 | 7c24f5f2… | 381 | 200 |
| 12 | ab230997… | 185 | 200 |

## Model cost per published Meal

**Assumption:** input $0.25 per 1M tokens, output $1.50 per 1M tokens (standard
paid tier). The free tier currently bills nothing, so these are the costs once the
project moves to the paid tier.

### Per-fixture token counts (text only)

| Fixture | Prompt tokens | Output tokens | Thinking tokens | Total | Latency (ms) |
|---|---|---|---|---|---|
| prompt injection inside macarona bechamel | 2046 | 234 | — | 2280 | 2633 |
| burger with transliterated English | 2028 | 208 | — | 2236 | 1256 |
| panee with mixed Latin and Arabic script | 2034 | 208 | — | 2242 | 1232 |
| nothing usable from the Cook | 2013 | 79 | — | 2092 | 1110 |
| koshary, plainly described | 2031 | 219 | — | 2250 | 1732 |
| stuffed vegetables mahshi | 2037 | 252 | — | 2289 | 1332 |
| molokhia with chicken and rice | 2028 | 262 | — | 2290 | 1461 |
| om ali dessert | 2024 | 209 | — | 2233 | 1640 |

**Mean across 8 fixtures:**
- Prompt tokens: 2030.1
- Output tokens: 208.9
- Total tokens: 2239.0

### Image call

The image is a flat solid grey at 1600×1200 — the dimensions the app uses when
picking photos (`maxWidth: 1600`, `imageQuality: 85`). Token cost is a function of
dimensions, not content. This is NOT a picture of food; Kafoo's rules forbid
synthetic food imagery.

- **Prompt tokens**: 3110
- **Output tokens**: 263
- **Total tokens**: 3373
- **Latency**: 2011 ms
- **Raw usage object**: `{"promptTokenCount":3110,"candidatesTokenCount":263,"totalTokenCount":3373,"promptTokensDetails":[{"modality":"TEXT","tokenCount":2046},{"modality":"IMAGE","tokenCount":1064}],"serviceTier":"standard"}`

### Cost per published Meal

**Without a photo** (one analysis call, mean of 8 fixtures):
- **Per Meal**: $0.000821
- **Per 1,000 published Meals**: $0.82

**With a photo** (two analysis calls — text mean + one image call):
- **Per Meal**: $0.001993
- **Per 1,000 published Meals**: $1.99

### Raw usage objects (for human verification)

**prompt injection inside macarona bechamel:**

```json
{
  "promptTokenCount": 2046,
  "candidatesTokenCount": 234,
  "totalTokenCount": 2280,
  "promptTokensDetails": [
    {
      "modality": "TEXT",
      "tokenCount": 2046
    }
  ],
  "serviceTier": "standard"
}
```

**burger with transliterated English:**

```json
{
  "promptTokenCount": 2028,
  "candidatesTokenCount": 208,
  "totalTokenCount": 2236,
  "promptTokensDetails": [
    {
      "modality": "TEXT",
      "tokenCount": 2028
    }
  ],
  "serviceTier": "standard"
}
```

**panee with mixed Latin and Arabic script:**

```json
{
  "promptTokenCount": 2034,
  "candidatesTokenCount": 208,
  "totalTokenCount": 2242,
  "promptTokensDetails": [
    {
      "modality": "TEXT",
      "tokenCount": 2034
    }
  ],
  "serviceTier": "standard"
}
```

**nothing usable from the Cook:**

```json
{
  "promptTokenCount": 2013,
  "candidatesTokenCount": 79,
  "totalTokenCount": 2092,
  "promptTokensDetails": [
    {
      "modality": "TEXT",
      "tokenCount": 2013
    }
  ],
  "serviceTier": "standard"
}
```

**koshary, plainly described:**

```json
{
  "promptTokenCount": 2031,
  "candidatesTokenCount": 219,
  "totalTokenCount": 2250,
  "promptTokensDetails": [
    {
      "modality": "TEXT",
      "tokenCount": 2031
    }
  ],
  "serviceTier": "standard"
}
```

**stuffed vegetables mahshi:**

```json
{
  "promptTokenCount": 2037,
  "candidatesTokenCount": 252,
  "totalTokenCount": 2289,
  "promptTokensDetails": [
    {
      "modality": "TEXT",
      "tokenCount": 2037
    }
  ],
  "serviceTier": "standard"
}
```

**molokhia with chicken and rice:**

```json
{
  "promptTokenCount": 2028,
  "candidatesTokenCount": 262,
  "totalTokenCount": 2290,
  "promptTokensDetails": [
    {
      "modality": "TEXT",
      "tokenCount": 2028
    }
  ],
  "serviceTier": "standard"
}
```

**om ali dessert:**

```json
{
  "promptTokenCount": 2024,
  "candidatesTokenCount": 209,
  "totalTokenCount": 2233,
  "promptTokensDetails": [
    {
      "modality": "TEXT",
      "tokenCount": 2024
    }
  ],
  "serviceTier": "standard"
}
```

**IMAGE: 1600×1200 solid gray (flat colour, not food):**

```json
{
  "promptTokenCount": 3110,
  "candidatesTokenCount": 263,
  "totalTokenCount": 3373,
  "promptTokensDetails": [
    {
      "modality": "TEXT",
      "tokenCount": 2046
    },
    {
      "modality": "IMAGE",
      "tokenCount": 1064
    }
  ],
  "serviceTier": "standard"
}
```

## Alongside this: E1's per-verification cost, still open

**This script did not measure it, and the figures below are list prices rather than
measurements.** They are here because a per-Meal cost is only half of what a Cook costs to
serve, and the other half is larger.

Signing in is phone OTP (`sign_in_screen.dart` calls `signInWithOtp(phone:)`), delivered by
Twilio Verify per E1's research. Published list prices, read on 2026-08-05:

- Twilio Verify: **$0.05 per successful verification**, plus channel fees
- Twilio SMS to Egypt: **$0.3959 per message**
- So roughly **$0.45 per successful sign-in**, and a resend costs another message

Set against the measured Meal cost, that is the number that decides things: **one sign-in
costs about as much as 550 published Meals without a photo, or 225 with one.** Publishing
Meals is close to free. Letting people sign in is not.

This does not close E1's T073, and should not be recorded as though it had. A list price is
not a delivered message: E1's research asks for real per-verification cost into Egyptian
networks, which needs a real handset on a real Egyptian network — the same spike that answers
whether sender-ID registration lets the message arrive at all. An undelivered SMS can still
be billed.

## What this does not tell you

- **Mobile network latency.** Runs execute from a cloud container, not an Egyptian
  mobile network. A real Cook on 3G or congested 4G will pay higher latency on top
  of these numbers.
- **Widget rebuild time.** The timer stops when the HTTP response is read. The Flutter
  framework still needs to parse the JSON, rebuild the widget tree, and paint the
  estimate on screen.
- **Other model tiers.** Only the fast tier is measured. The reasoning tier would
  cost more and take longer.
- **Provider switch.** If `AI_PROVIDER` changes, the model — and therefore the token
  counts and latency — will change. Re-run this script after any provider switch.
- **Edge Function cold start.** The first run in each batch may include a cold start.
  This is recorded, not discarded, because it is a real Cook's experience. But a
  frequently-used project may have warm functions, making later runs more typical.
- **Concurrent load.** These runs are serial. Under concurrent load the Edge Function,
  PostgREST, and the model provider may behave differently.
- **Image content.** The image token cost is measured with a flat grey square. Real
  photos contain detail that may affect tokenisation, though the dominant factor is
  dimensions.

