# Secrets
.env
.env.*
!.env.example

# Claude Code — personal overrides, never committed
.claude/settings.local.json
.claude/agent-memory-local/

# Flutter / Dart
.dart_tool/
build/
*.g.dart
*.freezed.dart
pubspec.lock

# Web surface (apps/web) — Next.js on Cloudflare, ADR-0008 Amendment 1
node_modules/
apps/web/.next/
apps/web/.open-next/
apps/web/next-env.d.ts
apps/web/.wrangler/
apps/web/tsconfig.tsbuildinfo

# Supabase
supabase/.branches/
supabase/.temp/

# Python bytecode — the vendored ui-ux-pro-max skill ships Python scripts,
# so running it regenerates these next to the source.
__pycache__/
*.py[cod]

# IDE files generated by `melos bootstrap`
.idea/
*.iml

# A review agent's scratch harness, written into the working tree while it runs.
# Never a deliverable: unreviewed, and named for what it is.
apps/mobile/test/zz_probe_test.dart
